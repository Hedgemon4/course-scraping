# Old/Unused Functions & Code

# Loop using string matching
# i <- 0
# j <- 0
# for (item in waterloo_course_requirements$`Course Code`) {
#   i <- i + 1
#   j <- 0
#   for (category in categories) {
#     j <- j + 1
#     if (grepl(item, category)) {
#       requirement_categories[i] <- category_names[j]
#     }
#   }
# }
